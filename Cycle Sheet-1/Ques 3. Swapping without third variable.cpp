#include <stdio.h>
//VIT ID: 21BCS0167

int main(){
	int a,b;
	a = 12;
	b = 10;
	a = a+b;
	b = a - b;
	a = a - b;
	printf("Hence the numbers are swapped. The numbers are %d, %d.", a, b);
	return 0;
}
